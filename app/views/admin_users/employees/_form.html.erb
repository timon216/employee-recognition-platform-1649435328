<%= form_with(model: admin_users_employee) do |form| %>
  <% if admin_users_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_users_employee.errors.count, "error") %> prohibited this admin_users_employee from being saved:</h2>

      <ul>
        <% admin_users_employee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
