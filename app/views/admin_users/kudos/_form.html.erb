<%= form_with(model: [:admin_users, @kudo]) do |form| %>
  <% if admin_users_kudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_users_kudo.errors.count, "error") %> prohibited this admin_users_kudo from being saved:</h2>

      <ul>
        <% admin_users_kudo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
