<%= form_with(model: admins_employee) do |form| %>
  <% if admins_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_employee.errors.count, "error") %> prohibited this admins_employee from being saved:</h2>

      <ul>
        <% admins_employee.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
