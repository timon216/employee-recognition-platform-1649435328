<%= form_with(model: [:admins, @kudo]) do |form| %>
  <% if admins_kudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_kudo.errors.count, "error") %> prohibited this admins_kudo from being saved:</h2>

      <ul>
        <% admins_kudo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
